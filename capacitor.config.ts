import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.benjaminflorentin.timesheet',
  appName: 'TimeSheet',
  webDir: 'dist',
};

export default config;
